// format: Major.Minor.Patch.Build where Build starts with 1
ext.globalVersionName = "5.0.0.6"

ext.globalVersionCode = 172

// idea: replace with automatic code based on date.
// Downside: harder to manage release note files in fastlane.
// Up side: managing release note files in fastlane stinks.
//
// def computeVersionCode() {
//     return new Date().format("yyyyMMddHH").toInteger()
// }
// ext.globalVersionCode = computeVersionCode(ext.globalVersionName)
