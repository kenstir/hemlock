include ':core', ':hemlock_app', ':pines_app', ':cwmars_app', ':cool_app', ':acorn_app', ':noble_app', ':ntlc_app', ':sagecat_app'

// include hemlock-pro-assets if it exists
def dir = new File('hemlock-pro-assets')
if (dir.exists()) {
  include ':' + dir
}
