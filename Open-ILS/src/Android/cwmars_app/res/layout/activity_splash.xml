<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#ffffff"
                android:gravity="center_horizontal"
                android:orientation="vertical" >
    <ImageView
            android:id="@+id/logo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:src="@drawable/cwmars_logo"
            android:contentDescription="@string/ou_library_name"
            />

    <TextView
            android:id="@+id/action_in_progress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/logo"
            android:layout_marginTop="15dip"
            android:gravity="center"
            style="@style/textLarge"
            android:paddingTop="10dip" android:text="Signing in"/>

    <!--
    <ImageView
            android:id="@+id/activity_splash_progress_bar"

            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/action_in_progress"
            android:layout_centerHorizontal="true"
            android:padding="20dip" android:src="@drawable/christmas_tree"/>
    -->

    <ProgressBar
            android:id="@+id/activity_splash_progress_bar"
            style="@android:style/Widget.ProgressBar.Large.Inverse"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="20dip" android:layout_gravity="center_horizontal"
            android:layout_below="@+id/action_in_progress"
            android:layout_centerHorizontal="true"/>

    <Button
            android:id="@+id/activity_splash_retry_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/activity_splash_progress_bar"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="60dp"
            android:text="@string/retry_label"
            android:paddingLeft="25sp"
            android:paddingRight="25sp"
            android:visibility="visible" android:layout_gravity="center_horizontal"/>

</RelativeLayout>
